{
  "name": "render-svgs-with-puppeteer",
  "version": "0.0.1",
  "description": "Quick and dirty SVG renderer using modern (2021) puppeteer and other dependencies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "dev": "tsc --pretty --watch",
    "build": "tsc --pretty",
    "prepare": "npm run build",
    "pretest": "npm run build"
  },
  "author": "v21",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/md5": "^2.3.1",
    "jest": "^27.2.5",
    "md5": "^2.3.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "puppeteer": "^10.4.0"
  },
  "engines": {
    "node": ">= 7.6.0"
  }
}
